#include "parser.h"

Parser parser;

void initParser(TArray *tokens) {
	printf("%i", tokens->length);
};

Typed *walk(void) {
	return NULL;
}

Program *parse(TArray *tokens) {
	initParser(tokens);

	return NULL;
};
