module main;

extern fx printf(fmt: ptr<u8>, ...) i32;
extern fx scanf(fmt: ptr<u8>, ...) i32;

fx main() i32 {
  var a: i32;
  var b: i32;

  var limit: i32 = 100000;
  var i: i32 = 0;

  while (i < limit) {
    a = a + 3000000 else {
      printf("Overflow\n");
      // overflow in overflow test
      a + 3000000 else {
        printf("Double overflow\n");
        return 2;
      }
      return 1;
    }
    i = i + 1;
  }
  return 0;
}
